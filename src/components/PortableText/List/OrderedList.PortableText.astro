---
import { cn } from "~/lib/utils";

interface Props {
  node: {
    level: number;
  };
}

const isInnerLevel = Astro.props.node.level > 1;
---

<ol class={cn("list-decimal list-inside ps-5", isInnerLevel && "ps-5")}>
  <slot />
</ol>
